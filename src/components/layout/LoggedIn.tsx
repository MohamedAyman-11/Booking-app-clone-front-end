import { Box } from '@mui/material';
import MenuPopupState from './DropDownMenu';

const LoggedIn = () => {
  return (
    <Box>
      <MenuPopupState  />
    </Box>
  );
};

export default LoggedIn;
