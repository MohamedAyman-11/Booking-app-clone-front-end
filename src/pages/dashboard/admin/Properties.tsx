import {Box} from "@mui/material";
import AdminProperties from "../../../components/dashboard/admin/properties/AdminProperties.tsx";

const Properties = () => {
  return (
    <Box sx={{py: '20px', px: '20px'}}>
      <AdminProperties/>
    </Box>
  );
};

export default Properties;