import {Box} from "@mui/material";
import AddNewProperty from "../../../components/dashboard/host/add/AddNewProperty.tsx";

const AddProperty = () => {
  return (
    <Box sx={{p: '10px 0 30px'}}>
      <AddNewProperty/>
    </Box>
  );
};

export default AddProperty;